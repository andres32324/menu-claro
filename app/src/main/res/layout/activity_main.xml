<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:padding="24dp">

    <!-- Flecha atrás -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="←"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="24dp"/>

    <!-- Título -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Menu Claro 1"
        android:textSize="28sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_marginBottom="32dp"/>

    <!-- Botón Servicios con doble toque secreto -->
    <TextView
        android:id="@+id/tvServicios"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Servicios"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:background="@drawable/btn_servicios"
        android:layout_marginBottom="32dp"/>

    <!-- Panel secreto - invisible por defecto -->
    <LinearLayout
        android:id="@+id/layoutSecret"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="16dp">

        <!-- Botón ON/OFF circular -->
        <FrameLayout
            android:id="@+id/btnToggle"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="@drawable/btn_off"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/tvBtnStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="●"
                android:textSize="32sp"
                android:textColor="#E53935"
                android:layout_gravity="center"/>
        </FrameLayout>

        <!-- IP -->
        <TextView
            android:id="@+id/tvIp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="..."
            android:textSize="16sp"
            android:textColor="#888888"
            android:layout_marginBottom="4dp"/>

        <!-- Código de emparejamiento -->
        <TextView
            android:id="@+id/tvCode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="# ------"
            android:textSize="13sp"
            android:textColor="#555555"/>

    </LinearLayout>

</LinearLayout>
